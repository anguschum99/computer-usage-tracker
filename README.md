# Computer Usage Tracker

A simple Python program that monitors which application window is currently active
and logs how long you use each program.

## Features
- Detects the active window and program name
- Logs usage time to a local SQLite database
- Designed for Windows (can be extended later)

## How to Run
1. Install dependencies:
   ```bash
   pip install -r requirements.txt
